

const HeaderMain = ({onClick}: {onClick: any}) => {
    return (
        <div className="header-main" onClick={onClick}>
            BossaTransaction
        </div>
    )
}

export default HeaderMain;